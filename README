This app connects to an API through an API key and retrieves 
some data by unmarshalling it and converting it into POJO just 
as like the DAO classes and stores all this data into a MariaDB 
database.
In the end you can search the database by an end point in your browser.
